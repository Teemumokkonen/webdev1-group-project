// JavaScript source code

const { getContentType } = require('../../utils/render') 

describe('Issues', () => {
    //first
    it('As an admin I want to add new a product to the database.', async () => {
        expect(false).to.be.true;
    });
    //second
    it('As an admin I want to edit a product.', async () => {
        expect(false).to.be.true;
    });
    //third
    it('As an admin I want to delete a product.', async () => {
        expect(false).to.be.true;
    });

    //fourth
    it('As a customer I want to view my orders.', async () => {
        expect(false).to.be.true;
    });

    //fifth
    it('As an admin I want to view all orders.', async () => {
        expect(false).to.be.true;
    });

    //sixth
    it('As an admin/customer I want to view details of an order.', async () => {
        expect(false).to.be.true;
    });

    //seventh
    it('As a customer I want to make an order.', async () => {
        expect(false).to.be.true;
    });

    //eight
    it('Create a schema for products', async () => {
        expect(false).to.be.true;
    });

    //ninth

    it('Create a schema for orders', async () => {
        expect(false).to.be.true;
    });

    //tenth

    it('Create a schema for orderedItem', async () => {
        expect(false).to.be.true;
    });

});

describe('content types', () => {

    it('text/javascript', async () => {
        getContentType('js');
    });

    it('json', async () => {
        getContentType('json');
    });

    it('png', async () => {
        getContentType('png');
    });

    it('jpg', async () => {
        getContentType('jpg');
    });

    it('svg', async () => {
        getContentType('svg');
    });

    it('wav', async () => {
        getContentType('wav');
    });

    it('css', async () => {
        getContentType('css');
    });

});
    //first


